/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    // Fix for specific imports if needed, otherwise standard config
};

module.exports = nextConfig;
